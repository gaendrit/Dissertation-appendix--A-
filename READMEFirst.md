This branch contains the code for the diffusion model and traditional methods (_con.py). It also contains the scripts to train and sample from the diffusion model. It also contains training and sampling job scripts for both the diffusion model and traditional methods.
Read the other README file to train and sample from a diffusion model
Download packages from requirements(1).txt first
